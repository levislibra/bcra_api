# Secrets

Create these local files before starting the stack:

- `secrets/database_url`: PostgreSQL connection string
- `secrets/secret_token`: API token used by the application

Suggested permissions:

```bash
chmod 600 secrets/database_url secrets/secret_token
```
